
metadata:
    name: kernel-devel
    format: "Lava-Test-Shell Test Definition 1.0"
    description: "kernel-devel"
    maintainer:
        - fyuanz_2010@163.com
    scope:
        - linux
    os:
        - centos
        - debian
    devices:
        - d05
    level: 1
  
run:
    steps:
        - "cd auto-test/kernel/inux/kernel-devel; ./kernel-devel.sh; cd -"

parse:
    pattern: "^(?!.+ED)(?P<test_case_id>\\w+)\\s+(?P<result>\\w+)\\s+\\d$"
    fixupdict:
        FAIL: fail
        PASS: pass

