
metadata:
    name: nginx_basic_testing
    format: "Lava-Test-Shell Test Definition 1.0"
    description: "nginx basic testing"
    maintainer:
        - hongxin_228@163.com
    scope:
        - database
        - nginx
    os:
        - centos
        - ubuntu
        - opensuse
    devices:
        - d03
        - d05
    level: 1
    totalcase:
run:
    steps:
        - "cd auto-test/middleware/database/nginx; ./nginx.sh;"

parse:
   pattern: "^(?!.+ED)(?P<test_case_id>\\w+)\\s+(?P<result>\\w+)\\s+\\d$"
   fixupdict:
   FAIL: fail
   PASS: pass

